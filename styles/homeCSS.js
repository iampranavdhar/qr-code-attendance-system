import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  scrollViewStyle: {
    backgroundColor: 'white',
  },
  textTitle: {
    fontSize: 20,
    fontWeight: 'bold',
    color: 'black',
    textAlign: 'center',
    marginTop: 10,
  },
  
});
